# AI Prototype Prompt: 潮起东方 (Chaoshan Narrative Nav App)

## 1. 全局设计规范 (Global Design System)

- **核心色彩**：
  - 主色调 (Primary): `Teal-600` (青绿色，代表潮汕古韵)
  - 强调色 (Accent): `Red-500` (朱砂红，用于红点、印章、重要按钮)
  - 背景色 (Background): `Stone-50` (米白色，纸质质感)
- **UI 风格**：轻量化国潮风格，结合现代扁平化 UI。使用圆角卡片、毛玻璃效果 (Glassmorphism)。
- **布局框架**：移动端 H5/小程序布局，底部包含固定导航栏 (Tab Bar)。

## 1.2 数据架构特别说明 (Data Architecture Note)

- **多城市架构**：本项目包含三个城市篇章（揭阳、潮州、汕头）。
- **当前数据范围**：你读取到的 `侨批信物之谜.md` 文档仅对应 **【揭阳篇】(City: Jieyang)** 的内容。
- **数据结构要求**：在设计 `gameData.ts` 时，请务必预留 `cityId` 字段。将当前的剧本、POI 和任务数据封装在 `jieyang` 对象下，以便未来扩展 `chaozhou` 和 `shantou` 数据。

------

## 2. 页面详细提示词 (Page Prompts)

请根据以下描述构建 5 个核心页面。

### 页面一：首页 - 剧情导航地图 (Home - Map & HUD)

**功能描述**：这是应用的主界面。核心是一个全屏的地图，上面覆盖着游戏 UI 元素。用户在这里切换城市、跟随真实导航寻找剧情点。

**UI 组件构成**：

1. **底图 (Map Layer)**：
   - 全屏显示地图组件（类似于 Mapbox/高德 SDK 样式）。
   - [cite_start]地图上显示一条**蓝色的导航路线**，终点是一个跳动的**红色波纹圆点**（代表剧情触发点 [cite: 46, 131]）。
   - [cite_start]非导航区域覆盖一层半透明的白色迷雾 (Fog of War) [cite: 133]。

2. **顶部功能区 (Top HUD)**：
   - **顶部居中 - 城市切换器 (City Switcher)**：
     - 一个悬浮的胶囊按钮，显示 "📍 揭阳 ▼"。
     - *交互逻辑*：点击后弹窗显示“揭阳、潮州(未解锁)、汕头(未解锁)”选项。
   - **顶部左侧 - 角色状态**：
     - 悬浮胶囊卡片。
     - [cite_start]显示当前用户选择的 **Q版人物头像**（例如：陈灵儿）[cite: 47]。
     - 显示角色名称和等级（如 "Lv.3 捕快"）。
   - **顶部右侧 - 功能入口**：
     - **背包按钮**：古风锦囊图标，点击打开背包模态框。

3. **任务提示卡 (Mission Card)**：
   - 位于页面中上部或左侧。
   - 显示当前任务目标（例如：“前往揭阳学宫寻找林文渊”）。

4. **底部导航栏 (Bottom Tab Bar)**：
   - 包含两个标签：**“探秘” (Home, Active)** 和 **“我的” (Mine)**。

------

### 页面二：角色选择页 (Character Selection)

**功能描述**：用户初次进入或切换线路时使用，采用全屏轮播卡片展示不同角色。

**UI 组件构成**：

1. **全屏轮播 (Carousel)**：
   - 支持左右滑动切换角色。
   - [cite_start]**中心卡片**：展示高精度的动漫人物立绘（例如：身穿捕快服的女性角色“陈灵儿”，手持腰牌 [cite: 206]）。

2. **底部信息板 (Bottom Sheet)**：
   - **角色名**：大号字体显示“陈灵儿”。
   - **身份标签**：显示“揭阳学宫捕快 | 探案路线”。
   - [cite_start]**剧情简介**：一段小字，“寻找父母失踪的真相，集齐五大印章...” [cite: 206]。
   - **确认按钮**：醒目的红色按钮“开启旅程”。

------

### 页面三：NPC 剧情交互 (NPC Dialogue Overlay)

**功能描述**：当用户点击地图上的红点后触发。背景显示景点实景，前景显示 NPC 对话。

**UI 组件构成**：

1. **背景层**：
   - [cite_start]全屏显示高清景点图片（例如：揭阳学宫的大成殿 [cite: 1]）。

2. **NPC 层**：
   - [cite_start]屏幕中央显示 NPC “林文渊” (学者形象) 的半身立绘 [cite: 206]。

3. **对话框层 (Dialogue Box)**：
   - 位于屏幕底部 1/3 处，半透明黑色背景。
   - 左上角显示说话人名字 “林文渊”。
   - 中间显示剧情文本：“年轻人，想要通过这里，必须回答我的问题。”
   - [cite_start]右侧有一个小的“播放”按钮，图标为摄像机，点击触发“观看历史影像”功能 [cite: 55]。

4. **选项按钮 (Options)**：
   - 在对话框上方悬浮显示 2-3 个选项按钮。
   - 示例：“A. 行作揖礼 (正确)”、“B. 四处看看”。

------

### 页面四：背包与信物拼图 (Inventory & Puzzle)

**功能描述**：展示用户收集到的印章，并进行拼图解谜。

**UI 组件构成**：

1. **上半部分 - 拼图盘 (The Puzzle)**：
   - [cite_start]核心是一个 **2x2 的凹槽石盘** [cite: 206]。
   - [cite_start]左上角凹槽已被点亮，显示红色的“儒学文脉章”图案，并拼凑出“揭阳城楼”图案的左上角一角 [cite: 1]。
   - 其余 3 个格子为灰色锁定状态。
   - [cite_start]中心位置有一个圆形的锁定孔，标记“老爷保号” [cite: 1]。

2. **下半部分 - 物品栏 (Grid List)**：
   - 3列网格布局。
   - 展示已获得的物品图标（如印章、线索卡）。
   - 点击物品可弹出详情弹窗。

------

### 页面五：我的 (User Profile)

**功能描述**：个人中心，展示成就和设置。

**UI 组件构成**：

1. **头部卡片 (Header)**：
   - 展示用户头像、昵称、称号（“揭阳守护者”）。
   - 背景使用国潮云纹纹理。
2. **数据栏 (Stats)**：
   - 展示三个数字：探索里程、收集印章数 (1/5)、完成任务数。
3. **功能列表 (List)**：
   - **成就墙**：展示获得的勋章图标。
   - **排行榜**：查看当前排名。
   - **文创兑换**：将虚拟印章兑换为实物。

------

## 3. 给智能体的特别指令 (System Instructions)

- **交互逻辑**：请确保“首页地图”上的红点是可点击的，点击后通过 Modal 或路由跳转到“NPC 剧情交互”页面。
- **组件复用**：背包页面应该作为一个独立的模态框 (Modal) 或页面，可以通过首页顶部的“背包按钮”随时进入。
- **响应式**：确保所有按钮和交互区域适合手指触摸（Touch-friendly，最小高度 44px）。