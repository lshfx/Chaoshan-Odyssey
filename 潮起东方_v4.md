# AI Project Blueprint: 潮起东方 (Chaoshan Narrative Nav App)

> **当前项目阶段 (Current Phase): Phase 4 - LBS 寻路与罗盘系统**
> **Agent 指令**: 这是一个基于地理位置 (LBS) 的 RPG。请仔细阅读本文档。标记为 `[已完成]` 的部分**禁止修改**。请集中精力完成标记为 `[当前任务]` 的部分。

------

## 1. 全局项目状态 (Project Status Tracker)

| 模块/页面 | 状态 | 说明 |
| :--- | :--- | :--- |
| **基础架构** | ✅ 已完成 | Pinia, SCSS, CustomNavbar |
| **P1: 首页地图** | 🔄 **[需更新]** | 需增加：1.罗盘入口 2.位置控制器 3.路径规划线 |
| **P3: 对话组件** | 🔒 **已锁定** | 核心资产，严禁修改 |
| **P5: 我的页面** | ✅ 已完成 | |
| **P4: 背包模态框** | ✅ 已完成 | |
| **F1: 罗盘功能** | 🔥 **[当前任务]** | 只有在特定任务时开启，或通过首页入口进入 |
| **F2: 位置模拟** | 🔥 **[当前任务]** | 开发环境专用的“位置控制器” |

------

## 2. 新增功能详细设计 (Feature Specs)

### [当前任务] 功能一：国潮罗盘页面 (Compass Page)
*页面路径：`pages/compass/index.vue`*

**功能描述**：
一个沉浸式的全屏工具页。利用手机的罗盘传感器 (`uni.onCompassChange`)，指引玩家前往目标点。

**UI 设计 (Oriental Cyberpunk)**：
1.  **背景**：深色星空或水墨流体背景。
2.  **核心组件 (罗盘盘面)**：
    * **底盘**：一张精美的“风水罗盘”或“八卦图”图片 (`static/compass-bg.png`)。
    * **指针**：一个金色的勺子（司南）或指针，根据传感器数据旋转。
    * **目标指示**：盘面上有一个红色的光点，指示当前任务目标的方位。
3.  **数据面板**：
    * 底部显示：当前朝向 (例如：北偏东 30°)、目标距离 (例如：剩 120 米)。

**逻辑要求**：
* 进入页面时开始监听罗盘。
* 离开页面时 (`onUnload`) 必须停止监听以省电。

------

### [当前任务] 功能二：上帝模式/位置控制器 (Location Debugger)
*组件路径：`components/LocationController.vue`* (仅开发环境显示)

**功能描述**：
为了方便在电脑前测试 LBS 功能，我们需要一个悬浮面板来“瞬移”玩家位置。

**UI 组件**：
* **外观**：一个半透明的黑色小面板，位于屏幕**左下角**。
* **内容**：
    * 显示当前模拟经纬度。
    * **方向键**：上/下/左/右 按钮 (点击后经纬度微调 0.0001 度)。
    * **预设点按钮**：
        * "传送：揭阳学宫"
        * "传送：进贤门"
        * "传送：青狮文化区"
* **逻辑**：点击按钮更新 `gameStore.userLocation`，并触发地图中心移动。

------

### [当前任务] 功能三：地图寻路与入口 (Map Pathfinding)

**修改页面：`pages/index/index.vue`**

**1. 新增 UI 入口**：
* **罗盘按钮**：在地图**右上角** (与背包、测试按钮同列)。
    * 图标：🧭 (或指南针 Icon)。
    * 功能：点击跳转 `pages/compass/index`。
    * *布局顺序 (Top-Right)*：[City Switcher] -> [Compass] -> [Backpack] -> [Test]。

**2. 路径规划逻辑 (Pathfinding)**：
* **数据源**：监听 `gameStore.userLocation` 和 `gameStore.targetLocation`。
* **渲染**：
    * 使用 `<map>` 组件的 `:polyline` 属性。
    * **画线逻辑**：由于暂无地图 API Key，请实现一个 **Mock 寻路**：
        * 直接绘制一条从 `userLocation` 到 `targetLocation` 的直线（虚线样式）。
        * 颜色：`#00897B` (Teal)，宽度 4px，带箭头图标。
* **交互**：
    * 当玩家使用“位置控制器”移动时，地图上的蓝色定位点 (User Marker) 应实时移动。
    * 当玩家距离目标点 < 50米 时，自动弹出提示“已到达目的地”。

------

## 3. 开发指令 (Execution Steps)

请按以下顺序执行代码编写：

1.  **Store 更新**：
    * 在 `gameStore` 中增加 `userLocation` (经纬度对象) 和 `targetLocation` (当前追踪的任务点)。
    * 增加 `updateUserLocation(lat, lng)` action。

2.  **组件开发**：
    * 创建 `LocationController.vue`。
    * 创建 `pages/compass/index.vue`。

3.  **首页集成**：
    * 在首页引入位置控制器。
    * 添加罗盘入口按钮。
    * 实现 `<map>` 的 `polyline` 动态绑定逻辑。