# AI Project Blueprint: 潮起东方 (Chaoshan Narrative Nav App)

> **当前项目阶段 (Current Phase): Phase 5 - AR 实景交互与沉浸式剧情**
> **Agent 指令**: 本文档定义了“AR 实景交互”的核心逻辑。这是一个基于地理位置 (LBS) 的文旅 RPG。请仔细阅读，并严格按照下方的 **Dev Plan** 分步执行。

------

## 1. 全局项目状态 (Project Status Tracker)

| 模块/页面 | 状态 | 说明 |
| :--- | :--- | :--- |
| **基础架构** | ✅ 已完成 | Pinia, SCSS, CustomNavbar |
| **P1: 首页地图** | ✅ 已完成 | LBS定位, 罗盘, 动态路径, 剧情联动Marker |
| **P2: 位置控制器**| ✅ 已完成 | 开发环境调试工具 (LocationController) |
| **P3: 对话组件** | ✅ 已完成 | `StoryDialogue.vue` (核心资产) |
| **P6: AR 交互页** | 🔥 **[当前任务]** | 新增页面 `pages/ar/index`，用于 NPC 实景互动 |
| **F1: 权限降级** | 🔥 **[当前任务]** | 处理摄像头权限拒绝或模拟器环境时的背景降级方案 |
| **S1: 剧情逻辑** | 🔥 **[当前任务]** | 任务完成后的状态更新与路由跳转 |

------

## 2. 新增功能详细设计 (Feature Specs)

### [核心任务] 功能：AR 实景剧情页 (AR Interaction Page)
*页面路径：`pages/ar/index.vue`*

**设计理念**：
当用户到达指定 POI（如揭阳学宫）并点击地图上的红点时，进入此页面。系统调用摄像头作为背景，将 NPC 立绘“贴”在现实场景中，模拟轻量级 WebAR 体验。

**UI 层级架构 (Z-Index Strategy)**：

1.  **底层 (Layer 0 - Background)**:
    * **首选方案**: `<camera>` 组件 (全屏，`mode="normal"`, `device-position="back"`)。
    * **降级方案 (Fallback)**: `<image>` 组件 (全屏，`mode="aspectFill"`)。
    * *触发降级条件*: 用户拒绝摄像头授权 OR 运行在开发者工具模拟器中。
    * *降级资源*: 显示该地点的精美实景照片 (从 `gameData` 的 `poi.background` 获取)。

2.  **中间层 (Layer 1 - NPC)**:
    * **NPC 立绘**: 使用 `<image>` 显示当前剧情对应的 NPC (如林文渊)。
    * *布局*: 绝对定位居中，底部对齐，高度约占屏幕 60%-70%。

3.  **顶层 (Layer 2 - UI)**:
    * **剧情对话**: 复用 `<StoryDialogue>` 组件。
    * **退出按钮**: 左上角悬浮的“退出实景”按钮 (半透明圆角)。

**逻辑流程 (Logic Flow)**：

1.  **初始化 (`onLoad`)**:
    * 接收路由参数: `npcId` (NPC ID), `poiId` (地点 ID)。
    * 检测运行环境: `uni.getSystemInfoSync().platform`.
    * 请求权限: `uni.authorize({ scope: 'scope.camera' })`.
2.  **渲染决策**:
    * 若权限获取成功且非模拟器 -> `isCameraAuthorized = true` (显示 Camera)。
    * 否则 -> `isCameraAuthorized = false` (显示静态背景图)。
3.  **剧情执行**:
    * 通过 `gameStore` 获取 NPC 数据和对话脚本。
    * 自动播放对话 (`gameStore.isDialogueVisible = true`)。
4.  **任务结算**:
    * 监听 `<StoryDialogue>` 的 `@dialogue-end` 事件。
    * 触发 Store Action: `completeMission(poiId)`。
    * 弹出提示 "探索完成"，延迟 1.5秒后返回首页。

------

## 3. 开发计划 (Dev Plan)

请严格按以下顺序生成代码，每一步完成后请暂停并确认。

### Step 1: 准备 AR 页面架构 (`pages/ar/index.vue`)
* **目标**: 创建 AR 页面骨架，实现“摄像头/背景图”的智能切换逻辑。
* **输入**: 无。
* **输出**: `pages/ar/index.vue` 完整代码。
* **详细要求**:
    * 使用 `<script setup lang="ts">`。
    * 定义响应式变量 `isCameraAuth` (boolean) 和 `bgImage` (string)。
    * 编写 `checkCameraPermission` 函数：在 `onLoad` 中调用。
    * **模拟器检测**: 如果 `platform === 'devtools'`，直接降级，不请求权限（避免报错）。
    * 布局: 使用 absolute/fixed 定位，确保背景铺满屏幕。

### Step 2: 集成对话与数据逻辑 (`pages/ar/index.vue`)
* **目标**: 加载 NPC 数据并复用对话组件。
* **输入**: 依赖 `gameStore`, `gameData`, `StoryDialogue.vue`。
* **输出**: 更新后的 `pages/ar/index.vue`。
* **详细要求**:
    * 引入 `<StoryDialogue />` 组件。
    * 根据 `npcId` 加载 NPC 立绘 (`npcImage`) 和 脚本 (`currentScript`)。
    * **重要**: 设置 `StoryDialogue` 的 `bgImage` prop 为空字符串，使其背景透明。
    * 实现 `@dialogue-end` 处理逻辑：Toast 提示 -> Store 更新 -> 返回。

### Step 3: 更新首页触发逻辑 (`pages/index/index.vue`)
* **目标**: 让地图上的 Marker 点击事件能够跳转到 AR 页面。
* **输入**: 修改 `onMarkerTap` 方法。
* **输出**: 更新后的 `pages/index/index.vue` (仅 Script 部分)。
* **详细要求**:
    * 获取点击的 POI 数据。
    * **距离校验**: 计算 `userLocation` 与 `poiLocation` 的距离。
        * 若距离 > 100米 (放宽一点方便测试): `uni.showToast({ title: '太远了，请走近一点', icon: 'none' })`。
        * 若距离达标: `uni.navigateTo({ url: `/pages/ar/index?npcId=${poi.npcId}&poiId=${poi.id}` })`。
    * *Debug 特权*: 如果是开发环境 (`import.meta.env.DEV`)，允许长按或直接跳转忽略距离。

### Step 4: Store 状态更新 (`stores/useGameStore.ts`)
* **目标**: 增加任务完成的逻辑处理。
* **输入**: `useGameStore.ts`。
* **输出**: 更新后的 Store 代码。
* **详细要求**:
    * 新增 Action: `completeMission(poiId)`.
    * 逻辑:
        1.  将 `poiId` 加入 `completedPoiIds` (需新增 State)。
        2.  解锁下一个剧情阶段 (修改 `storyStage` 或解锁下一个 POI)。
        3.  更新 `unlockedPoiIds`，移除当前 POI，添加下一个 POI (根据游戏流)。

------

## 4. 特别注意事项 (Crucial Notes)

* **Z-Index 管理**: 
    * `<camera>`: z-index: 1
    * `<image class="npc">`: z-index: 10
    * `<StoryDialogue>`: z-index: 999 (确保覆盖一切)
* **资源路径**: 请确保 `gameData.ts` 中的 `npc.avatar` 和 `poi.background` 路径是有效的。如果图片缺失，请在 `<image>` 标签中使用 `@error` 事件设置默认图。
* **真机调试**: Camera 组件只能在真机预览中看到真实效果，模拟器中将始终展示背景图 (Fallback)，这是预期行为。