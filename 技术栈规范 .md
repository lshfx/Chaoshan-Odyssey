## 1.1 技术栈规范 (Tech Stack Specification)

为了确保代码的现代性、可维护性以及 AI 生成的准确性，请严格遵守以下技术栈：

* **核心框架**: UniApp (基于 Vue 3 + Vite)。
* **脚本语法**: TypeScript + `<script setup>` (Composition API)。
* **UI 布局**:
    * **严禁**使用未安装的第三方 UI 库（如 Wot Design）。
    * 请直接使用 UniApp 内置组件 (`<view>`, `<image>`, `<text>`, `<map>`)。
    * 样式使用 **SCSS** 编写，采用 Flexbox 布局。
* **状态管理**: Pinia (用于管理 UserRole, Inventory, MissionProgress 等全局状态)。
* **地图组件**: 直接使用 UniApp 内置 `<map>` 组件。
    * *注意*: 涉及 LBS 逻辑时，请使用 `uni.getLocation` 和 `uni.openLocation` API。
* **路由管理**: 使用原生的 `uni.navigateTo` 进行页面跳转。

**代码生成要求**:
请尽量以 **单文件组件 (.vue)** 的形式输出代码，将 Template, Script, Style 整合在一起，方便我直接复制使用。